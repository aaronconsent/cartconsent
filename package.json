{
  "name": "cartconsent",
  "private": true,
  "version": "0.0.0",
  "description": "CartConsent monorepo — marketing site + WooCommerce plugin.",
  "scripts": {
    "build": "cd site && npm install --no-audit --no-fund && npm run build",
    "deploy": "npm run build && wrangler deploy"
  }
}
